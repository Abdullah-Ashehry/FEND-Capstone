import { createCard } from "../client/js/app.js";

describe("Testing the createCard functionality", () => {
    test("Testing the createCard() function", () => {
        expect(createCard).toBeDefined();
    });
});